import 'package:flutter/material.dart';

class EyeSpecialistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eye Specialist Detail'),
      ),
      body: Center(
        child: Text('Details for the Eye Specialist'),
      ),
    );
  }
}
